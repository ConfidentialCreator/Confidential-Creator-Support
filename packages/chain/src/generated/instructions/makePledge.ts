/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { CCSUPPORT_PROGRAM_ADDRESS } from "../programs";

export const MAKE_PLEDGE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  235, 47, 156, 254, 0, 88, 212, 142,
]);

export function getMakePledgeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MAKE_PLEDGE_DISCRIMINATOR);
}

export type MakePledgeInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountSupporter extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountPledge extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSupporter extends string
        ? WritableSignerAccount<TAccountSupporter> &
            AccountSignerMeta<TAccountSupporter>
        : TAccountSupporter,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountPledge extends string
        ? WritableAccount<TAccountPledge>
        : TAccountPledge,
      TAccountInstructions extends string
        ? ReadonlyAccount<TAccountInstructions>
        : TAccountInstructions,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MakePledgeInstructionData = {
  discriminator: ReadonlyUint8Array;
  periods: number;
  showPublicly: boolean;
};

export type MakePledgeInstructionDataArgs = {
  periods: number;
  showPublicly: boolean;
};

export function getMakePledgeInstructionDataEncoder(): FixedSizeEncoder<MakePledgeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["periods", getU8Encoder()],
      ["showPublicly", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: MAKE_PLEDGE_DISCRIMINATOR }),
  );
}

export function getMakePledgeInstructionDataDecoder(): FixedSizeDecoder<MakePledgeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["periods", getU8Decoder()],
    ["showPublicly", getBooleanDecoder()],
  ]);
}

export function getMakePledgeInstructionDataCodec(): FixedSizeCodec<
  MakePledgeInstructionDataArgs,
  MakePledgeInstructionData
> {
  return combineCodec(
    getMakePledgeInstructionDataEncoder(),
    getMakePledgeInstructionDataDecoder(),
  );
}

export type MakePledgeAsyncInput<
  TAccountSupporter extends string = string,
  TAccountConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountPledge extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  supporter: TransactionSigner<TAccountSupporter>;
  config?: Address<TAccountConfig>;
  creator: Address<TAccountCreator>;
  pledge: Address<TAccountPledge>;
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
  periods: MakePledgeInstructionDataArgs["periods"];
  showPublicly: MakePledgeInstructionDataArgs["showPublicly"];
};

export async function getMakePledgeInstructionAsync<
  TAccountSupporter extends string,
  TAccountConfig extends string,
  TAccountCreator extends string,
  TAccountPledge extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CCSUPPORT_PROGRAM_ADDRESS,
>(
  input: MakePledgeAsyncInput<
    TAccountSupporter,
    TAccountConfig,
    TAccountCreator,
    TAccountPledge,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MakePledgeInstruction<
    TProgramAddress,
    TAccountSupporter,
    TAccountConfig,
    TAccountCreator,
    TAccountPledge,
    TAccountInstructions,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CCSUPPORT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    supporter: { value: input.supporter ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    pledge: { value: input.pledge ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("config", accounts.config),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("pledge", accounts.pledge),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMakePledgeInstructionDataEncoder().encode(
      args as MakePledgeInstructionDataArgs,
    ),
    programAddress,
  } as MakePledgeInstruction<
    TProgramAddress,
    TAccountSupporter,
    TAccountConfig,
    TAccountCreator,
    TAccountPledge,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type MakePledgeInput<
  TAccountSupporter extends string = string,
  TAccountConfig extends string = string,
  TAccountCreator extends string = string,
  TAccountPledge extends string = string,
  TAccountInstructions extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  supporter: TransactionSigner<TAccountSupporter>;
  config: Address<TAccountConfig>;
  creator: Address<TAccountCreator>;
  pledge: Address<TAccountPledge>;
  instructions?: Address<TAccountInstructions>;
  systemProgram?: Address<TAccountSystemProgram>;
  periods: MakePledgeInstructionDataArgs["periods"];
  showPublicly: MakePledgeInstructionDataArgs["showPublicly"];
};

export function getMakePledgeInstruction<
  TAccountSupporter extends string,
  TAccountConfig extends string,
  TAccountCreator extends string,
  TAccountPledge extends string,
  TAccountInstructions extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CCSUPPORT_PROGRAM_ADDRESS,
>(
  input: MakePledgeInput<
    TAccountSupporter,
    TAccountConfig,
    TAccountCreator,
    TAccountPledge,
    TAccountInstructions,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MakePledgeInstruction<
  TProgramAddress,
  TAccountSupporter,
  TAccountConfig,
  TAccountCreator,
  TAccountPledge,
  TAccountInstructions,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CCSUPPORT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    supporter: { value: input.supporter ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
    pledge: { value: input.pledge ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.instructions.value) {
    accounts.instructions.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("config", accounts.config),
      getAccountMeta("creator", accounts.creator),
      getAccountMeta("pledge", accounts.pledge),
      getAccountMeta("instructions", accounts.instructions),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMakePledgeInstructionDataEncoder().encode(
      args as MakePledgeInstructionDataArgs,
    ),
    programAddress,
  } as MakePledgeInstruction<
    TProgramAddress,
    TAccountSupporter,
    TAccountConfig,
    TAccountCreator,
    TAccountPledge,
    TAccountInstructions,
    TAccountSystemProgram
  >);
}

export type ParsedMakePledgeInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    supporter: TAccountMetas[0];
    config: TAccountMetas[1];
    creator: TAccountMetas[2];
    pledge: TAccountMetas[3];
    instructions: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: MakePledgeInstructionData;
};

export function parseMakePledgeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMakePledgeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      supporter: getNextAccount(),
      config: getNextAccount(),
      creator: getNextAccount(),
      pledge: getNextAccount(),
      instructions: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMakePledgeInstructionDataDecoder().decode(instruction.data),
  };
}
