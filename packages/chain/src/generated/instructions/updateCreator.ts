/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findCreatorPda } from "../pdas";
import { CCSUPPORT_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_CREATOR_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  39, 221, 251, 213, 194, 161, 31, 207,
]);

export function getUpdateCreatorDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CREATOR_DISCRIMINATOR,
  );
}

export type UpdateCreatorInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountWallet extends string
        ? ReadonlySignerAccount<TAccountWallet> &
            AccountSignerMeta<TAccountWallet>
        : TAccountWallet,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateCreatorInstructionData = {
  discriminator: ReadonlyUint8Array;
  name: string;
  description: string;
  suggestedAmount: bigint;
};

export type UpdateCreatorInstructionDataArgs = {
  name: string;
  description: string;
  suggestedAmount: number | bigint;
};

export function getUpdateCreatorInstructionDataEncoder(): Encoder<UpdateCreatorInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["description", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["suggestedAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_CREATOR_DISCRIMINATOR }),
  );
}

export function getUpdateCreatorInstructionDataDecoder(): Decoder<UpdateCreatorInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["description", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["suggestedAmount", getU64Decoder()],
  ]);
}

export function getUpdateCreatorInstructionDataCodec(): Codec<
  UpdateCreatorInstructionDataArgs,
  UpdateCreatorInstructionData
> {
  return combineCodec(
    getUpdateCreatorInstructionDataEncoder(),
    getUpdateCreatorInstructionDataDecoder(),
  );
}

export type UpdateCreatorAsyncInput<
  TAccountWallet extends string = string,
  TAccountCreator extends string = string,
> = {
  wallet: TransactionSigner<TAccountWallet>;
  creator?: Address<TAccountCreator>;
  name: UpdateCreatorInstructionDataArgs["name"];
  description: UpdateCreatorInstructionDataArgs["description"];
  suggestedAmount: UpdateCreatorInstructionDataArgs["suggestedAmount"];
};

export async function getUpdateCreatorInstructionAsync<
  TAccountWallet extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof CCSUPPORT_PROGRAM_ADDRESS,
>(
  input: UpdateCreatorAsyncInput<TAccountWallet, TAccountCreator>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateCreatorInstruction<TProgramAddress, TAccountWallet, TAccountCreator>
> {
  // Program address.
  const programAddress = config?.programAddress ?? CCSUPPORT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.creator.value) {
    accounts.creator.value = await findCreatorPda(
      {
        wallet: getAddressFromResolvedInstructionAccount(
          "wallet",
          accounts.wallet.value,
        ),
      },
      { programAddress },
    );
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("creator", accounts.creator),
    ],
    data: getUpdateCreatorInstructionDataEncoder().encode(
      args as UpdateCreatorInstructionDataArgs,
    ),
    programAddress,
  } as UpdateCreatorInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountCreator
  >);
}

export type UpdateCreatorInput<
  TAccountWallet extends string = string,
  TAccountCreator extends string = string,
> = {
  wallet: TransactionSigner<TAccountWallet>;
  creator: Address<TAccountCreator>;
  name: UpdateCreatorInstructionDataArgs["name"];
  description: UpdateCreatorInstructionDataArgs["description"];
  suggestedAmount: UpdateCreatorInstructionDataArgs["suggestedAmount"];
};

export function getUpdateCreatorInstruction<
  TAccountWallet extends string,
  TAccountCreator extends string,
  TProgramAddress extends Address = typeof CCSUPPORT_PROGRAM_ADDRESS,
>(
  input: UpdateCreatorInput<TAccountWallet, TAccountCreator>,
  config?: { programAddress?: TProgramAddress },
): UpdateCreatorInstruction<TProgramAddress, TAccountWallet, TAccountCreator> {
  // Program address.
  const programAddress = config?.programAddress ?? CCSUPPORT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    wallet: { value: input.wallet ?? null, isWritable: false },
    creator: { value: input.creator ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("creator", accounts.creator),
    ],
    data: getUpdateCreatorInstructionDataEncoder().encode(
      args as UpdateCreatorInstructionDataArgs,
    ),
    programAddress,
  } as UpdateCreatorInstruction<
    TProgramAddress,
    TAccountWallet,
    TAccountCreator
  >);
}

export type ParsedUpdateCreatorInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    wallet: TAccountMetas[0];
    creator: TAccountMetas[1];
  };
  data: UpdateCreatorInstructionData;
};

export function parseUpdateCreatorInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateCreatorInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { wallet: getNextAccount(), creator: getNextAccount() },
    data: getUpdateCreatorInstructionDataDecoder().decode(instruction.data),
  };
}
