/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { CCSUPPORT_PROGRAM_ADDRESS } from "../programs/index.ts";

export const SET_VISIBILITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  182, 23, 73, 166, 255, 234, 36, 196,
]);

export function getSetVisibilityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_VISIBILITY_DISCRIMINATOR,
  );
}

export type SetVisibilityInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountSupporter extends string | AccountMeta<string> = string,
  TAccountPledge extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSupporter extends string
        ? ReadonlySignerAccount<TAccountSupporter> &
            AccountSignerMeta<TAccountSupporter>
        : TAccountSupporter,
      TAccountPledge extends string
        ? WritableAccount<TAccountPledge>
        : TAccountPledge,
      ...TRemainingAccounts,
    ]
  >;

export type SetVisibilityInstructionData = {
  discriminator: ReadonlyUint8Array;
  showPublicly: boolean;
};

export type SetVisibilityInstructionDataArgs = { showPublicly: boolean };

export function getSetVisibilityInstructionDataEncoder(): FixedSizeEncoder<SetVisibilityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["showPublicly", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_VISIBILITY_DISCRIMINATOR }),
  );
}

export function getSetVisibilityInstructionDataDecoder(): FixedSizeDecoder<SetVisibilityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["showPublicly", getBooleanDecoder()],
  ]);
}

export function getSetVisibilityInstructionDataCodec(): FixedSizeCodec<
  SetVisibilityInstructionDataArgs,
  SetVisibilityInstructionData
> {
  return combineCodec(
    getSetVisibilityInstructionDataEncoder(),
    getSetVisibilityInstructionDataDecoder(),
  );
}

export type SetVisibilityInput<
  TAccountSupporter extends string = string,
  TAccountPledge extends string = string,
> = {
  supporter: TransactionSigner<TAccountSupporter>;
  pledge: Address<TAccountPledge>;
  showPublicly: SetVisibilityInstructionDataArgs["showPublicly"];
};

export function getSetVisibilityInstruction<
  TAccountSupporter extends string,
  TAccountPledge extends string,
  TProgramAddress extends Address = typeof CCSUPPORT_PROGRAM_ADDRESS,
>(
  input: SetVisibilityInput<TAccountSupporter, TAccountPledge>,
  config?: { programAddress?: TProgramAddress },
): SetVisibilityInstruction<
  TProgramAddress,
  TAccountSupporter,
  TAccountPledge
> {
  // Program address.
  const programAddress = config?.programAddress ?? CCSUPPORT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    supporter: { value: input.supporter ?? null, isWritable: false },
    pledge: { value: input.pledge ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("pledge", accounts.pledge),
    ],
    data: getSetVisibilityInstructionDataEncoder().encode(
      args as SetVisibilityInstructionDataArgs,
    ),
    programAddress,
  } as SetVisibilityInstruction<
    TProgramAddress,
    TAccountSupporter,
    TAccountPledge
  >);
}

export type ParsedSetVisibilityInstruction<
  TProgram extends string = typeof CCSUPPORT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    supporter: TAccountMetas[0];
    pledge: TAccountMetas[1];
  };
  data: SetVisibilityInstructionData;
};

export function parseSetVisibilityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetVisibilityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { supporter: getNextAccount(), pledge: getNextAccount() },
    data: getSetVisibilityInstructionDataDecoder().decode(instruction.data),
  };
}
