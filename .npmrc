# Peer dependencies are not auto-installed: @solana-mobile/wallet-adapter-mobile
# declares react-native as a peer, and with auto-install on pnpm pulled the whole
# react-native 0.87 into a browser SPA, with peer conflicts on react 19.
auto-install-peers=false
strict-peer-dependencies=false
